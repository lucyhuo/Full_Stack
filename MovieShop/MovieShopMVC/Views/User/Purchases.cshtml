@model ApplicationCore.Models.PurchaseResponseModel
<!--List Model -->
<!--  Model = movieCards -->

@{
    ViewData["Title"] = "Purchased Movies";
}

<div class="rounded">
    <div class="container-fluid">
        <div class="row">
            @foreach (var movieCard in Model.PurchasedMovies)
            {
            <div class="col-xl-2 col-lg-3 col-sm-4 col-6">

                <partial name="_MovieCard" model="movieCard" />
                @*@await Html.PartialAsync("_movieCard", movieCard);*@
            <div class="imgButton">
                <a asp-controller="User" asp-action="PurchaseDetails" asp-route-movieId="@movieCard.Id" class="btn btn-secondary">Purchase Details</a>
                
            </div>
            </div>
                
            }
        </div>
    </div>
</div>


